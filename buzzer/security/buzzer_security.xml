<?xml version="1.0" encoding="utf-8"?>
<odoo>

<data noupdate="1">

    <record id="buzzer_channel_rule_portal" model="ir.rule">
        <field name="name">Portal: read only its buzzer channels with animator</field>
        <field name="model_id" ref="model_buzzer_channel"/>
        <field name="domain_force">[('user_ids','in', [user.id]), ('user_id', '!=', False)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
    </record>

</data>

</odoo>
